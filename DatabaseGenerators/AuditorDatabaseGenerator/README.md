# AuditorDatabaseGenerator

This is a simple single source file project that defines the Foundation Auditor system's database.

This is a *.Net Standard class library that uses *CodeGenerationCommon*'s database generator to implement the Auditor system's tables.

It is to be used by any application on either .Net Core or .Net framework that cares about the Auditor's schema definition.