# SecurityDatabaseGenerator

This is a simple single source file project that defines the Foundation Security system's database.

This is a *.Net Standard class library that uses *CodeGenerationCommon*'s database generator to implement the Security system's tables.

It is to be used by any application on either .Net Core or .Net framework that cares about the Security system's schema definition.