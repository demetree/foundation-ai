# CodeGenerationCore

This is a .Net 9 class library that extends CodeGenerationCommon to add *.Net Core* specific features, and to add the ability to generate 
code that is intended to work in .Net Core.

It is to be referenced by Foundation *Core* projects, particularly those that generate the application web controller code.
