# Fix My Collection Part Colour Rendering

- [x] Normalize hex in `my-collection.component.ts` (`updateDisplayedParts`, `getThumbnailKey`, `getColourStyle`)
- [x] Add defensive `#`-stripping in `ldraw-thumbnail.service.ts` `applyColourOverride()`
- [x] Build and verify — Angular build passes
