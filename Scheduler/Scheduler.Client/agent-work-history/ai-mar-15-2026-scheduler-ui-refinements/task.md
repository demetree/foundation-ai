# Scheduler.Client UI Refinement

## Audit
- [ ] Check Scheduler styles.scss for global Bootstrap overrides (card, table, bg-white, table-light, etc.)
- [ ] Scan component SCSS for remaining `--bs-*` variables
- [ ] Check sidebar-accessible components for layout margin consistency
- [ ] Visually inspect the running app on port 5901

## Fixes
- [ ] Add global Bootstrap overrides to Scheduler styles.scss (matching Foundation pattern)
- [ ] Replace `--bs-*` with `--sch-*` in component SCSS files
- [ ] Standardize layout margins across sidebar components
- [ ] Build verify
